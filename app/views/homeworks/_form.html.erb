<%= form_with(model: homework, local: true) do |form| %>
  <% if homework.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(homework.errors.count, "error") %> prohibited this homework from being saved:</h2>

      <ul>
      <% homework.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :class_group_id %>
    <%= form.collection_select(:class_group_id, ClassGroup.order(:year, :name), :id, :name, prompt: true ) %></p>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_area :title %>
  </div>

  <div class="field">
    <%= form.label :is_worksheet %>
    <%= form.check_box :is_worksheet %>
  </div>

  <div class="field">
    <%= form.label :misconception %>
    <%= form.text_area :misconception %>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
