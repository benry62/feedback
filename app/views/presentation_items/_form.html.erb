<%= form_with(model: presentation_item, local: true) do |form| %>
  <% if presentation_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(presentation_item.errors.count, "error") %> prohibited this presentation_item from being saved:</h2>

      <ul>
      <% presentation_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= form.label :heading %>
    <%= form.text_field :heading %>
  </div>

  <div class="field">
    <%= form.label :category %>
    <%= form.text_field :category %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
